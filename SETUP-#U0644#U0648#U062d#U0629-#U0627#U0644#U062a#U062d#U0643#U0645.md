# لوحة التحكم — دليل التفعيل (خطوة مرة واحدة بس)

## الطريقة الأساسية والأسهل: فولدر لكل مشروع

الموقع دلوقتي بيقرا مشاريعه تلقائيًا من شكل الفولدرات جوّه `images/projects-by-name`.
معندكش داعي لأي لوحة تحكم أو Cloudflare إعدادات عشان تضيف أو تعدّل مشروع — بس فولدرات وصور.

### إزاي تضيف مشروع جديد
1. روح لمجلد `images/projects-by-name/<القسم>/` — فيه 3 فولدرات:
   - `تصميم-داخلي`
   - `تصميم-خارجي`
   - `تصميم-جرافيك`
2. جوّه القسم المناسب، اعمل فولدر جديد باسم المشروع **بالظبط زي ما عايزه يظهر على الموقع**
3. حط جوّاه صور المشروع — **أول صورة بالترتيب الأبجدي بتبقى صورة الغلاف**، عشان كده سمّي الصور بأرقام (`01-...`, `02-...`) لو عايز تتحكم في الترتيب
4. (اختياري) اعمل ملف اسمه `info.txt` جوّه نفس الفولدر، بالشكل ده:
   ```
   العنوان: صالة استقبال حديثة
   التصنيف: شقق سكنية
   الموقع: مدينة نصر
   السنة: 2025
   المساحة: 90 م²
   الوصف: وصف المشروع بالتفصيل...
   الفكرة: الفكرة التصميمية اللي بنيت عليها المشروع...
   الخامات: خشب طبيعي, رخام, إضاءة مخفية
   الوسوم: Modern, Living Room
   ```
   لو ملقتش الملف ده، الموقع هيحط قيم بسيطة تلقائية وتقدر تضيفه بعدين براحتك.
5. من GitHub Desktop: Commit ثم Push — الموقع هيتحدث لوحده تلقائي خلال دقيقة أو اتنين

### إزاي تعدّل مشروع موجود
- **تغيير الاسم:** غيّر اسم الفولدر نفسه
- **إضافة صورة:** حط الصورة جوّه الفولدر
- **حذف صورة:** امسحها من الفولدر
- **تعديل التفاصيل:** عدّل ملف `info.txt`

### إزاي تمسح مشروع
- امسح الفولدر بتاعه بالكامل

### خطوة تفعيل واحدة بس على Cloudflare
عشان التوليد التلقائي ده يشتغل، لازم تظبط "أمر البناء" مرة واحدة:
1. روح لمشروع الـ Pages بتاعك → **Settings → Build**
2. في **Build command** اكتب:
   ```
   node scripts/build-projects.js
   ```
3. Save — وأي رفعة جديدة بعد كده هتشغّل السكريبت ده تلقائيًا قبل ما ينشر الموقع

---

## الطريقة الثانية (اختيارية): لوحة تحكم فيها تسجيل دخول

لو حابب كمان تقدر تعدّل من على الموبايل بسرعة من غير GitHub، أو تحب شكل الفورم
والأزرار، فيه لوحة تحكم حقيقية على `/admin.html` — دي مش بديلة للطريقة الأولى،
الاتنين بيشتغلوا مع بعض. الإعداد بتاعها محتاج الخطوات اللي تحت.

---

## 1) إنشاء مكان لتخزين بيانات المشاريع (KV)

1. من لوحة Cloudflare، روح لـ **Workers & Pages → KV**
2. دوس **Create a namespace**
3. سمّيها مثلاً: `rk-projects`
4. احفظ

---

## 2) ربط الـ KV بموقعك

1. روح لمشروع الـ Pages بتاعك → **Settings → Functions**
2. لاقي قسم **KV namespace bindings**
3. دوس **Add binding**
   - **Variable name:** `PROJECTS_KV`  ⚠️ (لازم يكون بالظبط كده، حروف كبيرة)
   - **KV namespace:** اختار `rk-projects` اللي عملتها فوق
4. احفظ

---

## 3) إنشاء مكان لتخزين الصور (R2) — اختياري

> ⚠️ **ملاحظة:** Cloudflare بتطلب منك تضيف وسيلة دفع (فيزا) قبل ما تقدر تستخدم R2،
> حتى لو هتفضل في الحد المجاني. لو مش جاهز دلوقتي، **من غير مشكلة** — اللوحة
> فيها حقل بديل تكتب فيه مسار الصورة يدويًا (بعد ما ترفعها بنفسك عن طريق
> GitHub زي الأول)، وتقدر ترجع تفعّل الرفع التلقائي بعدين لما تجهز.
> لو هتستخدم الحقل اليدوي، تقدر تتخطى الخطوتين 3 و4 دول تمامًا.

1. من لوحة Cloudflare، روح لـ **R2**
2. دوس **Create bucket**
3. سمّيه مثلاً: `rk-project-images`
4. بعد ما يتعمل، ادخل عليه → **Settings → Public access**
5. فعّل **Allow public access** (أو اعمل ربط بـ custom domain لو حابب)
6. هتلاقي رابط عام للـ bucket شكله شبه:
   `https://pub-xxxxxxxxxxxx.r2.dev`
   احفظ الرابط ده، هتحتاجه في الخطوة الجاية

---

## 4) ربط الـ R2 بموقعك (لو عملته)

1. ارجع لمشروع الـ Pages → **Settings → Functions**
2. لاقي قسم **R2 bucket bindings**
3. دوس **Add binding**
   - **Variable name:** `PROJECT_IMAGES` ⚠️ (بالظبط كده)
   - **R2 bucket:** اختار `rk-project-images`
4. احفظ

لو مش عملت R2 دلوقتي، تخطى الخطوة دي — اللوحة هتشتغل عادي بالحقل اليدوي لمسارات الصور.

---

## 5) كلمة سر اللوحة + رابط الصور

لسه في نفس صفحة **Settings**:

1. روح لـ **Environment variables**
2. ضيف متغيّرين (Add variable) لكل البيئات (Production + Preview):
   - `ADMIN_PASSWORD` = اختار كلمة سر قوية بنفسك (ده اللي هتدخل بيه على `/admin.html`)
   - `R2_PUBLIC_URL` = الرابط اللي نسخته في الخطوة 3 (مثال: `https://pub-xxxxxxxxxxxx.r2.dev`)
3. احفظ، وأعد النشر (Redeploy) للموقع مرة عشان الإعدادات تتفعّل

---

## 6) أول استخدام للوحة

1. افتح `yourdomain.com/admin.html`
2. سجّل دخول بكلمة السر اللي اخترتها
3. أول ما تدخل هيظهرلك زرار **"استيراد بيانات الموقع الحالية"** — دوسه **مرة واحدة بس**
   (ده بينقل كل الـ 46 مشروع الموجودين دلوقتي جوّه اللوحة، عشان تقدر تعدّل/تمسح منهم بعد كده)
4. من هنا وطالع: أي إضافة أو تعديل أو حذف بتظهر على الموقع فورًا

---

## ملاحظات مهمة

- **الأمان:** كلمة السر هي الحماية الوحيدة — اختارها قوية ومتشاركهاش مع حد. أي حد يعرفها يقدر يعدّل/يمسح مشاريع.
- **الصور القديمة:** الصور اللي كانت موجودة في `images/projects/` هتفضل شغالة عادي زي ما هي — مش محتاج تنقلها. أي صورة جديدة ترفعها من اللوحة هتتخزن في R2 تلقائيًا.
- **لو نسيت تعمل الإعدادات دي:** الموقع مش هيتكسر — هيفضل شغال عادي بالبيانات الحالية (الموجودة أصلاً في الكود)، وبس زرار اللوحة مش هيشتغل لحد ما تخلّص الإعدادات.
- **لو محتاج تغيّر كلمة السر:** ارجع لـ Environment Variables وغيّر قيمة `ADMIN_PASSWORD`، وأعد النشر.
